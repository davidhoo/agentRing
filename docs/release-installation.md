下载 DMG，退出 Agent Ring 后，将 `AgentRing.app` 拖入「应用程序」并选择替换。

旧版（不含 Sparkle）的用户需要手动覆盖安装一次；后续可在应用内点击「检查更新」，由 Sparkle 验证更新签名、安装并重启。

此版本使用 ad-hoc 应用签名和 EdDSA 更新签名，未经 Apple 公证。首次打开可能需要前往「系统设置 → 隐私与安全性」选择「仍要打开」。Sparkle 的更新签名不等于 Apple 开发者身份认证。
